[project]
name = "havl-research"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "matplotlib>=3.8",
  "numpy>=1.26",
  "pandas>=2.1",
  "scikit-learn>=1.4",
  "scipy>=1.12",
  "seaborn>=0.13",
  "torch>=2.2",
  "tqdm>=4.66",
]

[tool.pytest.ini_options]
pythonpath = ["src"]

